#include<iostream>
#include<vector>
using namespace std;
int check(vector<int>arr){
    int i;int si=0,a;
    int ei=arr.size()-1;
        while(si<=ei){
            int mid=(si+ei)/2;
            if(arr[mid]==no){
             
            }
            
            else if(arr[mid]<no)
            si=mid+1;
            else ei=mid-1;
        }
    return false;
}
